/** Matches --source-0 … --source-7 in index.css (feeds without `color` in ics.json). */
export const SOURCE_PALETTE = [
  '#4a8fff',
  '#7fba00',
  '#e040fb',
  '#ff7043',
  '#26c6da',
  '#ab47bc',
  '#ffca28',
  '#8d6e63',
] as const;
